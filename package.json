
{
  "name": "react-portfolio",
  "version": "1.0.0",
  "description": "A professional portfolio application built with React.",
  "main": "index.tsx",
  "scripts": {
    "test:e2e": "playwright test",
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html && esbuild index.tsx --bundle --outfile=dist/index.js --loader:.tsx=tsx"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "^7.9.4",
    "@heroicons/react": "^2.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "axe-playwright": "^2.2.2",
    "typescript": "^5.4.5",
    "esbuild": "^0.23.0"
  }
}
