# 🧠 AI‑генератор UI — архитектура и техническое руководство

## Введение

AI‑генератор UI — это практическая система, преобразующая текстовые промты пользователя в работающий код интерфейса (HTML, CSS, JS).  
Она анализирует смысл запроса и автоматически создаёт нужные фрагменты, без ручного написания кода.  
Документ предназначен для разработчиков, инженеров и аналитиков, которым важно понять внутреннее устройство пайплайна.

---

## 1. Цель и концепция

Цель системы — автоматизировать создание клиентского кода и повысить воспроизводимость интерфейсов.  
Основная идея — построить **универсальный промт‑роутер**, который:

- определяет тип задачи (CSS, HTML/JS, System Codes);
- маршрутизирует запрос к нужному обработчику;
- объединяет результаты в единый финальный ответ.

Система работает полностью на стороне клиента, не генерирует серверный код и строится по принципу модульной архитектуры.

---

## 2. Общая архитектура пайплайна

```
User Prompt
   ↓
[ prompt.md ] — маршрутизатор
   ↓
 ┌───────────────────┬──────────────────┬────────────────────┐
 │ CSS Handler       │ HTML/JS Generator│ System Code Parser │
 │ cssOnlyHandler.md │ generateHTMLJS.md│ system_code_parser │
 └───────────────────┴──────────────────┴────────────────────┘
   ↓
 validateSystemCodes
   ↓
 post‑processing (prompt v2.ini)
   ↓
 Final JSON → { answer, map }
```

---

## 3. Основные файлы и их назначение

| Файл | Назначение | Ключевая логика |
|------|-------------|----------------|
| **`README.md`** | Корневое описание пайплайна | Содержит общую структуру и принципы работы |
| **`prompt.md`** | Роутер | Анализирует запрос, определяет режим, вызывает нужный обработчик |
| **`cssOnlyHandler.md`** | Генератор CSS | Отвечает только за стили, без изменения HTML; при неясности задаёт уточняющий вопрос |
| **`system_code_parser.md`** | Парсер системных кодов | Извлекает токены `$CODE$` из корпуса; ограничивает вывод до 3; при ошибке вызывает `askUserIntent` |
| **`generateHTMLJS.md`** | Генератор HTML/JS | Создаёт готовый клиентский код; добавляет `<script>`; избегает серверной логики |
| **`prompt v2.ini`** | Пост‑обработка | Удаляет служебные следы и нормализует ответ |
| **`/parser/context/V01…V08`** | Корпус контекста | Таблицы вида `<Module>|<Template>|<$CODE$>|<Description>;;` |
| **`/parser/context/prompt.txt`** | Регламент контекстов | Формат и правила работы с корпусом |

---

## 4. Как это работает внутри

### 4.1 Этап анализа (`resolveAndRender`)

1. NLP‑парсинг текста запроса.
2. Извлечение признаков: `module`, `template`, `filename`, `question`.
3. Определение режима (по ключевым словам: `css`, `stylesheet`, `html`, `template`).
4. Передача данных в нужный модуль.

### 4.2 CSS‑режим

1. Извлекается селектор (`.button`, `#header` и т. д.).  
2. Проверяются ключевые эффекты (`stars`, `glow`, `snow`, `confetti`).  
3. Если эффект найден — возвращается готовый CSS.  
   Если нет — система задаёт уточнение:  
   _«Какие свойства нужно изменить для `<selector>`?»_

### 4.3 System Codes‑режим

1. Извлекаются коды из корпуса по `module`, `template`, `filename`.  
2. Ограничение: максимум 3 кода.  
3. Результат в JSON:  
   ```json
   {
     "system_codes": ["$GLOBAL_PROMO$", "$GLOBAL_FAQ$"],
     "descriptions": ["Глобальный промо‑блок", "Глобальный блок FAQ"]
   }
   ```
4. Если ничего не найдено — активируется `askUserIntent`.

### 4.4 HTML + JS‑режим

1. Получает HTML и описание задачи.  
2. Добавляет JavaScript‑логику через `<script>`.  
3. Код выполняется на `DOMContentLoaded`.  
4. При отсутствии селектора вставляется комментарий `// TODO`.  
5. Никаких библиотек и серверных частей.

---

## 5. Поток данных и связи модулей

- `prompt.md` → определяет режим и создаёт `parsedData`.
- `cssOnlyHandler` получает только текст и селектор.
- `system_code_parser` возвращает токены для `generateHTMLJS`.
- `validateSystemCodes` проверяет результат (в текущей версии — заглушка).
- После завершения всё объединяется в финальный JSON `{ answer, map }`.

Поле `map` служит только для логирования и QA, не отображается пользователю.

---

## 6. Пример полного запроса

**Запрос:**  
> «Сделай блок отзывов с кнопкой “Показать больше”»

**Ход выполнения:**  
1. `prompt.md` → определяет режим = HTML+JS.  
2. Создаёт `parsedData` с вопросом и пустыми полями кодов.  
3. `generateHTMLJS` → строит HTML‑фрагмент и добавляет JS‑логику.  
4. `prompt v2.ini` → очищает вывод и форматирует JSON.  
5. Финальный ответ содержит `answer` (код) и `map` (список источников).

**Выход:**  
```html
<section class="reviews">
  <h2>Отзывы</h2>
  <div class="review">...</div>
  <button id="showMore">Показать больше</button>
</section>
<script>
document.addEventListener('DOMContentLoaded', () => {
  const btn = document.getElementById('showMore');
  btn.onclick = () => document.querySelectorAll('.review.hidden').forEach(el => el.classList.remove('hidden'));
});
</script>
```

---

## 7. Обработка ошибок

| Тип ошибки | Поведение системы |
|-------------|------------------|
| Не найден CSS‑селектор | Система задаёт уточняющий вопрос |
| Не найден системный код | Активируется `askUserIntent` |
| Несоответствие формата JSON | Возвращается пустой `{system_codes: [], descriptions: []}` |
| Серверная логика в запросе | Добавляется `// TODO`, генерация продолжается |

---

## 8. Диагностика и отладка

Система поддерживает диагностический режим, в котором формируется служебный блок:

```json
{
  "diagnostic": {
    "источник": ["system_code_parser.md", "V03.txt"],
    "анализ": "2 кода валидны, 1 пропущен",
    "решение": "добавлен уточняющий вопрос",
    "время_обработки": "62ms"
  }
}
```

Этот блок используется для внутренних тестов, QA и автоматического контроля корректности ответов.

---

## 9. Принципы проектирования

1. **Изоляция контекстов.** Каждый модуль независим, без пересечений логики.  
2. **Прозрачность.** Все результаты сериализуются в JSON.  
3. **Минимализм.** Только необходимый код, без внешних зависимостей.  
4. **Проверяемость.** Каждый шаг может быть протестирован отдельно.  
5. **Расширяемость.** Новые режимы добавляются без изменения базового пайплайна.

---

## 10. Возможности расширения

- Добавить **LLM‑router** для точного выбора режима.  
- Подключить **reference‑files API** для строгой валидации кодов.  
- Внедрить кеширование и анализ контекста.  
- Создать веб‑интерфейс (Playground) для визуального тестирования.  
- Интеграция с IDE и системой автотестов.

---

## 11. Заключение

AI‑генератор UI — это гибкая платформа для создания пользовательских интерфейсов по текстовым запросам.  
Она сочетает маршрутизацию, строгую валидацию и изоляцию контекстов, что делает её пригодной для интеграции в IDE, RAG‑системы, чат‑боты и внутренние инструменты автоматизации.

---

© 2025. Документация подготовлена для внутреннего использования.
